<nav class="navbar navbar-expand-lg navbar-light bg-white z-index-3 py-3 shadow-lg fixed-top">
	<div class="container">
		<a class="navbar-brand" href="{{ path('app_home') }}">
			Super Markit
		</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navigation"
				aria-controls="navigation" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navigation">
			<ul class="navbar-nav navbar-nav-hover mx-auto">
				<li class="nav-item px-3">
					<a class="nav-link" href="{{ path('app_home') }}">
						Home
					</a>
				</li>
				<li class="nav-item px-3">
					<a class="nav-link" href="{{ path('app_categories') }}">
						Categories
					</a>
				</li>
				<li class="nav-item px-3">
					<a class="nav-link" href="{{ path('app_home') }}">
						Product
					</a>
				</li>
				<li class="nav-item px-3">
					<a class="nav-link" href="{{ path('app_order') }}">
						Order
					</a>
				</li>
			</ul>
			<div class="d-flex justify-content-around align-items-center">
				<div class="mx-3">
					<a href="#"><i class="fa-solid fa-heart fa-xl mx-2 text-danger"></i></a>
					<a href="{{ path('app_shopping_basket') }}" class="position-relative">
						<i class="fa-solid fa-cart-shopping fa-xl mx-2">
							{% if numberOfProduct is defined and numberOfProduct > 0 %}
								<span class="fw-light position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"
									  style="font-size: 0.7rem;">
									+{{ numberOfProduct }}
								</span>
							{% endif %}
						</i>
					</a>
				</div>
				<div class="ms-4 d-flex align-items-center">
					{% if app.user %}
						<a href="#"><i class="fa-regular fa-circle-user fa-2xl mx-2 text-secondary"></i></a>
						<a class="icon-link icon-link-hover mb-0 ms-2" href="{{ path('app_logout') }}">
							Logout <i class="fa-solid fa-arrow-right-from-bracket"></i>
						</a>
					{% else %}
						<a class="btn btn-outline-dark mb-0 mx-2" href="{{ path('app_register') }}">sign up</a>
						<a class="btn btn-outline-primary mb-0" href="{{ path('app_login') }}">sign in</a>
					{% endif %}
				</div>
			</div>
		</div>
	</div>
</nav>
